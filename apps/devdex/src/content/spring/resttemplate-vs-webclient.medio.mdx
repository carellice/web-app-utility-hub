# RestTemplate vs WebClient — Pattern intermedi

## Timeout e retry

```java
@Bean
public WebClient web(WebClient.Builder b) {
    HttpClient http = HttpClient.create()
        .responseTimeout(Duration.ofSeconds(3))
        .option(ChannelOption.CONNECT_TIMEOUT_MILLIS, 2000);
    return b.clientConnector(new ReactorClientHttpConnector(http)).build();
}
```

Retry con Reactor:

```java
web.get().uri("/flaky")
    .retrieve()
    .bodyToMono(String.class)
    .retryWhen(Retry.backoff(3, Duration.ofMillis(200))
                    .filter(ex -> ex instanceof IOException));
```

## Filter / Interceptor

Aggiungere un header a tutte le richieste:

```java
WebClient web = WebClient.builder()
    .filter((req, next) -> {
        ClientRequest authed = ClientRequest.from(req)
            .header("Authorization", "Bearer " + tokens.get())
            .build();
        return next.exchange(authed);
    })
    .build();
```

Per RestTemplate, `ClientHttpRequestInterceptor` fa lo stesso lavoro.

## Gestione errori

```java
web.get().uri("/orders/{id}", id)
    .retrieve()
    .onStatus(HttpStatusCode::is4xxClientError,
              resp -> resp.bodyToMono(ApiError.class)
                          .flatMap(err -> Mono.error(new OrderNotFound(err))))
    .bodyToMono(Order.class);
```

## Test

- RestTemplate: `MockRestServiceServer` di `spring-test`.
- WebClient: `ExchangeFunction` mock o `MockWebServer` di OkHttp.

```java
ExchangeFunction stub = req -> Mono.just(
    ClientResponse.create(HttpStatus.OK)
        .header("Content-Type", "application/json")
        .body("{\"id\":1}")
        .build());
var web = WebClient.builder().exchangeFunction(stub).build();
```

## Connection pool

- RestTemplate (Apache HttpClient): pool con `PoolingHttpClientConnectionManager`.
- WebClient (Reactor Netty): `ConnectionProvider.builder("custom").maxConnections(100)...`

> ⚠️ Senza tuning, sotto carico il default ti **strozza**. Misura sempre.
